import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BandaWeb3 Surf Week | Puerto Escondido Mayo 2026",
  description: "Surf, Web3 y networking en Puerto Escondido durante ETH Cinco de Mayo Pop-Up City. Mayo 18-24, 2026.",
  openGraph: {
    title: "BandaWeb3 Surf Week",
    description: "Surf, Web3 y networking en Puerto Escondido",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
